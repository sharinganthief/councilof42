@model WebApplication1.Areas.Movie.Models.SeensModel

    <div class="pure-form pure-form-aligned">
        <fieldset>
            <div class="pure-control-group">
                @Html.LabelFor(o => o.Users)
                @Html.DropDownListFor(o => o.Users, new SelectList(Model.Users, Model.Users.Last()))
            </div>

        </fieldset>
    </div>
